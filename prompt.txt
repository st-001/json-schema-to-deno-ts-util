Great. In the generated typescript code, it looks like this at the top:
```typescript
import Ajv from "https://esm.sh/ajv@8.12.0";
import addFormats from "https://esm.sh/ajv-formats@2.1.1";

const ajv = initializeAjv();

const compressedSchema = {
  "$id": "CustomerCompressed",
  "type": "object",
  "description": "A customer.",
  "properties": {
    "1": {
```


Your task is this:
The const ajv = initializeAjv(); should only be used by the generator code. 
The generated code should build its own initializeAjv() function, and use that instead.